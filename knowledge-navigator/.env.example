# Base URL of the RippleBot backend API.
# Defaults to http://localhost:8000 if unset. Copy this file to `.env` and edit
# for other environments (staging, LAN IP, deployed backend, etc.).
VITE_API_URL=http://localhost:8000
